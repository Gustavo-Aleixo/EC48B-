<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>TODO APP</title>
  </head>
  <body>
    <h1 style="margin-bottom: 70px;">TODO APP</h1>
    
    <h2>Nova tarefa</h2>
    <form action="/tarefas" method="POST">
        {{#task.id}}
        <input type="hidden" name="id" value="{{.}}">
        {{/task.id}}
        <label for="nome">Nome:</label>
        <input type="text" id="nome" name="nome" value="{{task.name}}" required/>

        <label for="prioridade">Prioridade:</label>
        <select id="prioridade" name="prioridade" required>
            {{#priorities}}
            <option value="{{value}}" {{#isSelected}}selected{{/isSelected}}>{{value}}</option>
            {{/priorities}}
        </select>
        <input type="submit" value="Salvar" class="btn-primary"/>
    </form>
    <p>{{erro}}</p>

    <h2 style="margin-top: 70px;">Tarefas</h2>
    <ul>
    {{#tasks}}
        <li style="color: {{cor}};">
            {{name}} 
            <a href="/?tid={{id}}">edit</a> 
            <a href="/tarefas/del/{{id}}">done</a>
        </li>
    {{/tasks}}
    </ul>

    <div id="botoes" style="margin-top: 70px;">
        <a href="/tasks/count" class="btn btn-success">Ver número de tarefas cadastradas</a>
        <a href="/tasks/list" class="btn btn-secondary">Listar Tarefas</a>
    </div>
    

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>
</html>
